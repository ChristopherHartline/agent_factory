# Agent Factory Dependencies
# Python 3.10+ required

# LangChain ecosystem (unpinned - use latest)
langchain-core
langchain
langgraph

# Agent runtime (unpinned)
deepagents

# LLM providers (unpinned)
langchain-anthropic
langchain-openai

# Data handling
pyyaml

# Environment management
python-dotenv
